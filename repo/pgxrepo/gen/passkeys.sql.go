// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.28.0
// source: passkeys.sql

package pgxgen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createBackupCode = `-- name: CreateBackupCode :exec
INSERT INTO yauth_backup_codes (id, user_id, code_hash, used, created_at)
VALUES ($1, $2, $3, $4, $5)
`

type CreateBackupCodeParams struct {
	ID        string
	UserID    string
	CodeHash  string
	Used      bool
	CreatedAt pgtype.Timestamptz
}

func (q *Queries) CreateBackupCode(ctx context.Context, arg CreateBackupCodeParams) error {
	_, err := q.db.Exec(ctx, createBackupCode,
		arg.ID,
		arg.UserID,
		arg.CodeHash,
		arg.Used,
		arg.CreatedAt,
	)
	return err
}

const createPasskey = `-- name: CreatePasskey :exec
INSERT INTO yauth_webauthn_credentials (id, user_id, name, aaguid, device_name, credential, created_at, last_used_at)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8)
`

type CreatePasskeyParams struct {
	ID         string
	UserID     string
	Name       string
	Aaguid     *string
	DeviceName *string
	Credential string
	CreatedAt  pgtype.Timestamptz
	LastUsedAt pgtype.Timestamptz
}

func (q *Queries) CreatePasskey(ctx context.Context, arg CreatePasskeyParams) error {
	_, err := q.db.Exec(ctx, createPasskey,
		arg.ID,
		arg.UserID,
		arg.Name,
		arg.Aaguid,
		arg.DeviceName,
		arg.Credential,
		arg.CreatedAt,
		arg.LastUsedAt,
	)
	return err
}

const createTOTP = `-- name: CreateTOTP :exec
INSERT INTO yauth_totp_secrets (id, user_id, encrypted_secret, verified, created_at, last_used_step)
VALUES ($1, $2, $3, $4, $5, $6)
`

type CreateTOTPParams struct {
	ID              string
	UserID          string
	EncryptedSecret string
	Verified        bool
	CreatedAt       pgtype.Timestamptz
	LastUsedStep    *int64
}

func (q *Queries) CreateTOTP(ctx context.Context, arg CreateTOTPParams) error {
	_, err := q.db.Exec(ctx, createTOTP,
		arg.ID,
		arg.UserID,
		arg.EncryptedSecret,
		arg.Verified,
		arg.CreatedAt,
		arg.LastUsedStep,
	)
	return err
}

const deleteAllBackupCodesForUser = `-- name: DeleteAllBackupCodesForUser :execrows
DELETE FROM yauth_backup_codes WHERE user_id = $1
`

func (q *Queries) DeleteAllBackupCodesForUser(ctx context.Context, userID string) (int64, error) {
	result, err := q.db.Exec(ctx, deleteAllBackupCodesForUser, userID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const deletePasskey = `-- name: DeletePasskey :execrows
DELETE FROM yauth_webauthn_credentials WHERE id = $1
`

func (q *Queries) DeletePasskey(ctx context.Context, id string) (int64, error) {
	result, err := q.db.Exec(ctx, deletePasskey, id)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const deleteTOTPForUser = `-- name: DeleteTOTPForUser :execrows
DELETE FROM yauth_totp_secrets
WHERE user_id = $1
AND ($2::bool = false OR verified = true)
`

type DeleteTOTPForUserParams struct {
	UserID  string
	Column2 bool
}

func (q *Queries) DeleteTOTPForUser(ctx context.Context, arg DeleteTOTPForUserParams) (int64, error) {
	result, err := q.db.Exec(ctx, deleteTOTPForUser, arg.UserID, arg.Column2)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const getPasskeyByIDAndUser = `-- name: GetPasskeyByIDAndUser :one
SELECT id, user_id, name, aaguid, device_name, credential, created_at, last_used_at FROM yauth_webauthn_credentials WHERE id = $1 AND user_id = $2 LIMIT 1
`

type GetPasskeyByIDAndUserParams struct {
	ID     string
	UserID string
}

func (q *Queries) GetPasskeyByIDAndUser(ctx context.Context, arg GetPasskeyByIDAndUserParams) (YauthWebauthnCredential, error) {
	row := q.db.QueryRow(ctx, getPasskeyByIDAndUser, arg.ID, arg.UserID)
	var i YauthWebauthnCredential
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Name,
		&i.Aaguid,
		&i.DeviceName,
		&i.Credential,
		&i.CreatedAt,
		&i.LastUsedAt,
	)
	return i, err
}

const getPasskeysByUserID = `-- name: GetPasskeysByUserID :many
SELECT id, user_id, name, aaguid, device_name, credential, created_at, last_used_at FROM yauth_webauthn_credentials WHERE user_id = $1 ORDER BY created_at ASC
`

func (q *Queries) GetPasskeysByUserID(ctx context.Context, userID string) ([]YauthWebauthnCredential, error) {
	rows, err := q.db.Query(ctx, getPasskeysByUserID, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []YauthWebauthnCredential{}
	for rows.Next() {
		var i YauthWebauthnCredential
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.Name,
			&i.Aaguid,
			&i.DeviceName,
			&i.Credential,
			&i.CreatedAt,
			&i.LastUsedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getTOTPByUserID = `-- name: GetTOTPByUserID :one
SELECT id, user_id, encrypted_secret, verified, created_at, last_used_step FROM yauth_totp_secrets
WHERE user_id = $1
AND ($2::bool = false OR verified = true)
LIMIT 1
`

type GetTOTPByUserIDParams struct {
	UserID  string
	Column2 bool
}

func (q *Queries) GetTOTPByUserID(ctx context.Context, arg GetTOTPByUserIDParams) (YauthTotpSecret, error) {
	row := q.db.QueryRow(ctx, getTOTPByUserID, arg.UserID, arg.Column2)
	var i YauthTotpSecret
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.EncryptedSecret,
		&i.Verified,
		&i.CreatedAt,
		&i.LastUsedStep,
	)
	return i, err
}

const getUnusedBackupCodesByUserID = `-- name: GetUnusedBackupCodesByUserID :many
SELECT id, user_id, code_hash, used, created_at FROM yauth_backup_codes WHERE user_id = $1 AND used = false ORDER BY created_at ASC
`

func (q *Queries) GetUnusedBackupCodesByUserID(ctx context.Context, userID string) ([]YauthBackupCode, error) {
	rows, err := q.db.Query(ctx, getUnusedBackupCodesByUserID, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []YauthBackupCode{}
	for rows.Next() {
		var i YauthBackupCode
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.CodeHash,
			&i.Used,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const markBackupCodeUsed = `-- name: MarkBackupCodeUsed :execrows
UPDATE yauth_backup_codes SET used = true WHERE id = $1 AND used = false
`

func (q *Queries) MarkBackupCodeUsed(ctx context.Context, id string) (int64, error) {
	result, err := q.db.Exec(ctx, markBackupCodeUsed, id)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const markTOTPUsed = `-- name: MarkTOTPUsed :execrows
UPDATE yauth_totp_secrets
SET last_used_step = $2
WHERE id = $1
AND COALESCE(last_used_step, -1) < $2
`

type MarkTOTPUsedParams struct {
	ID           string
	LastUsedStep *int64
}

// MarkTOTPUsed only ever ADVANCES the replay counter: the COALESCE guard means
// a concurrent verification of an OLDER code cannot rewind it and re-open the
// newer step to replay. Zero rows affected therefore means either "row gone"
// or "already at/past this step" — both are fine outcomes for the caller.
func (q *Queries) MarkTOTPUsed(ctx context.Context, arg MarkTOTPUsedParams) (int64, error) {
	result, err := q.db.Exec(ctx, markTOTPUsed, arg.ID, arg.LastUsedStep)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const markTOTPVerified = `-- name: MarkTOTPVerified :execrows
UPDATE yauth_totp_secrets SET verified = true WHERE id = $1
`

func (q *Queries) MarkTOTPVerified(ctx context.Context, id string) (int64, error) {
	result, err := q.db.Exec(ctx, markTOTPVerified, id)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const updatePasskeyCredential = `-- name: UpdatePasskeyCredential :execrows
UPDATE yauth_webauthn_credentials SET credential = $2, last_used_at = $3 WHERE id = $1
`

type UpdatePasskeyCredentialParams struct {
	ID         string
	Credential string
	LastUsedAt pgtype.Timestamptz
}

// UpdatePasskeyCredential carries the post-assertion credential (crucially its
// sign counter) back to the row, so WebAuthn L3 §7.2 step 24 has something
// current to compare the NEXT assertion against. last_used_at moves in the same
// statement so the two can never drift apart. No migration: the credential
// column already exists.
func (q *Queries) UpdatePasskeyCredential(ctx context.Context, arg UpdatePasskeyCredentialParams) (int64, error) {
	result, err := q.db.Exec(ctx, updatePasskeyCredential, arg.ID, arg.Credential, arg.LastUsedAt)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const updatePasskeyLastUsed = `-- name: UpdatePasskeyLastUsed :execrows
UPDATE yauth_webauthn_credentials SET last_used_at = $2 WHERE id = $1
`

type UpdatePasskeyLastUsedParams struct {
	ID         string
	LastUsedAt pgtype.Timestamptz
}

func (q *Queries) UpdatePasskeyLastUsed(ctx context.Context, arg UpdatePasskeyLastUsedParams) (int64, error) {
	result, err := q.db.Exec(ctx, updatePasskeyLastUsed, arg.ID, arg.LastUsedAt)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}
